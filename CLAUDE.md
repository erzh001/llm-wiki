# LLM-wiki — верхний уровень (карта неймспейса)

Корень репозитория — **верхний** уровень (level) личного неймспейса: meta-vault
(`meta-vault/`) + домены + проекции, всё в одном репозитории. Это
папка-контейнер, не один проект. Терминология — `meta-vault/glossary.md`.

Финальная цель — **LLM-независимое хранилище**: данные = читаемый markdown,
переживающий любой инструмент. Движок/клиент выкидываемо-переписываем; данные
остаются. Кэш клиента (`~/.claude/` и т.п.) — сменный, **не** источник правды.

> **Инвариант (все уровни):** NEVER `AskUserQuestion` / popup-диалоги с
> вариантами — вопросы владельцу всегда обычным текстом. Это пример
> универсального инварианта — правила, которое повторяется в каждой двери,
> потому что наследования нет; владелец настраивает свой набор в
> `meta-vault/preferences.md`.

## Уровни неймспейса (иерархия)

Корень — верх: доступ ко всему, связи вниз через `meta-vault/`. Ниже — домены,
каждый в своём scope:

- **личное:** `life/` — живой первоисточник (дневник, тексты). `trust: primary`.
- **работа:** `study/` (учёба), `creative/` (творчество) — домены-примеры;
  переименуй и замени под свою жизнь, новые рождай из
  `meta-vault/vault-template.md`.

**Иерархия, не демократия:** top-агент пишет во все уровни ниже; нижние
горизонтально друг другу не пишут. Работая в нижнем уровне, агент подчиняется
**локальному** `CLAUDE.md` этого уровня.

## Оркестратор (агент этого уровня)

Имена агентам даёт владелец; реестр — `meta-vault/glossary.md` → «Имена
агентов». Режимы:

- **Governance** — пишет вниз во все уровни; нижние горизонтально не пишут.
- **Навигатор (read-mode)** — не знаешь, куда идти: спускается по уровням,
  находит дверь, маршрутизирует. Без записи.
- **Диспатч** — вызывает уровневых агентов как воркеров, **всегда с планом,
  который владелец читает** до запуска.
- Продукт **не** производит: работа над содержимым домена ведётся в самом
  домене, внешние артефакты — в проекции `public/`.

## Ось доверия

`trust` — метаданное первого класса. Граница папок = граница доверия.
Тест: **«я это написал/проверил — или ИИ надстроил поверх и я не глядел?»**

| Род | trust | Регенерируется? | Дом |
|---|---|---|---|
| Первоисточник | `primary` | нет (незаменимо) | `life/` |
| Производное ИИ | `derived` | да | (зоны появятся при надобности) |
| Движок (код+индекс) | — | версионируется в git | (не подключён; RAG опционален) |

## Раскладка корня

- `CLAUDE.md` — эта дверь (карта неймспейса).
- `meta-vault/` — meta-vault: метод, решения, связи вниз ко всем доменам.
  Старт — `HOME.md`.
- `life/` — **первоисточник** личного: `diary/`, `inbox/`, `texts/`.
  `trust: primary`, `sensitivity: private`; содержимое дневника — вне git.
- `study/` — домен учёбы: своя дверь + `study-vault/`.
- `creative/` — домен творчества: своя дверь + `creative-vault/` + `media/`
  (тяжёлые файлы, вне git).
- `public/` — проекция «наружу»: готовые внешние артефакты. Безагентная.
- `documents/` — приёмник документов (вне git).
- `secrets/` — доступы (вне git, NEVER commit/sync/index; контракт в README).

## Границы

- Не переименовывать уровни втихую и не переписывать их git-историю; смена
  имени домена — осознанный шаг с обновлением всех дверей и карт.
- Секреты только в `secrets/` / `.env`, никогда в трекаемых файлах — только
  указатели (`meta-vault/secrets-map.md`).
- Личное (дневник, inbox), медиа, документы и секреты в git не попадают по
  построению (`.gitignore`). Если репозиторий получает remote — держать его
  приватным, пока владелец явно не решил иначе.
- Тяжёлые файлы (аудио, видео, документы) живут в своих папках вне git;
  в vault — карточки-указатели.

> Канон структуры: `meta-vault/vault-template.md` (база-баз) +
> `meta-vault/entry-points.md` (двери/карты).

## Навигация (граф)

[[meta-vault/HOME|meta-vault]] · [[life/HOME|life]] ([[life/CLAUDE|дверь]]) ·
[[study/study-vault/HOME|study]] ([[study/CLAUDE|дверь]]) ·
[[creative/creative-vault/HOME|creative]] ([[creative/CLAUDE|дверь]]) ·
[[public/HOME|public]] ([[public/CLAUDE|дверь]]) ·
[[documents/README|documents]] · [[secrets/README|secrets]]
