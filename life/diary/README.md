# diary/ — ежедневник

Файлы `YYYY-MM-DD.md`, `type: diary`. Пишет владелец.

Содержимое этой папки **вне git** (см. корневой `.gitignore`) — трекается
только этот README. Дневник — личное: живёт локально и не покидает машину.
